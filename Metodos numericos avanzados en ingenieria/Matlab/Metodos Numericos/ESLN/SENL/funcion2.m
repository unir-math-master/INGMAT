function [fun,dfun] = f(x,y)
    %fun=(1-2*x)*y;
    %dfun=(1-2*x);
    %fun = (sin(x)).^2-x.^2+1;
    %dfun= 2.*sin(x).*cos(x);
    
    fun = x*exp(x.^2)+3*cos(x) = sin(x.^2) - 5;
    dfun= x*exp(x.^2)*(2)+exp(x.^2) -3*sin(x) = cos(x.^2)*(2) - 5;
end